{
  "name": "random-sensors",
  "version": "1.0.0",
  "description": "Random messages sent on a MQTT server",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "mqtt": "^2.0.1"
  }
}
